# app
app ci


Done:

- add multiarch docker images for AMD64 and ARM64

Next progress:

- will add version tag and "latest" on docker image
- will add observation middleware part on go app
- will add consumer app with python app
- will add message broker
- will add version modifier on cd part
